import { PageLoader } from "@/components/page-loader";

export default function Loading() {
  return <PageLoader label="Loading your courses…" detail="Preparing your learning space." kind="cards" />;
}
